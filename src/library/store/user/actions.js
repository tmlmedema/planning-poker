/*
 *
 * User Actions
 *
 */

import * as ActionTypes from './action-types';

export const login = (previousState, action) => {
    return()
};

export const logout = (previousState, action) => {

};

export const addUser = (previousState, action) => {

};

export const editUser = (previousState, action) => {

};

export const deleteUser = (previousState, action) => {

};

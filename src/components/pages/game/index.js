import React from 'react';
import PokerTemplate from 'components/templates/poker';

const Game = () => {

    return(
        <PokerTemplate>
            <h1>Game</h1>
        </PokerTemplate>

    );

};

export default Game;
